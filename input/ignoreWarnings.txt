== Suppressed Messages ==

# Add warning and/or information messages here after you've confirmed that they aren't really a problem
# (And include comments like this justifying why)
# See https://github.com/FHIR/sample-ig/blob/master/input/ignoreWarnings.txt for examples

# Faulty error in IG publisher, this link does work
The link 'https://dhp.uz/fhir/core/%

# SDC only publishes for R4 (no R5 package exists); we depend on it cross-version so the
# sdc-questionnaire-calculatedExpression used by the DMED screening questionnaires resolves - same suppression as in uz.dhp.core
This IG is version 5.0.0, while the IG 'hl7.fhir.uv.sdc' is from version 4.0.1
The ImplementationGuide is based on FHIR version 5.0.0 but package hl7.fhir.uv.sdc#3.0.0 is based on FHIR version 4.0.1. In general, this version mismatch should be avoided - some tools will try to make this work with variable degrees of success, but others will not even try
The ImplementationGuide uses package hl7.fhir.uv.sdc#3.0.0 released on 2022-03-08, but the most recent appropriate version is 4.0.0. This reference is getting old and the more recent version should be considered

# No external code system we use publishes Uzbek designations - not SNOMED CT (the DMED
# questionnaire answerOptions), not ISO 3166 (the jurisdiction UZ on every CodeSystem/ValueSet),
# not LOINC - so a uz-language resource necessarily falls back to the English canonical display.
# That is correct and unfixable on our side.
# Originally scoped to uz only, on the assumption that a ru gap was actionable via our local
# SNOMED CT supplement CodeSystems (e.g. SnomedUzSupplementCS, ScreeningObservationCodesCS). Verified
# empirically (2026-09-02) that this assumption doesn't hold when validating with an external -tx
# server (tx.fhir.org, as both CI and _genonce.sh do): this message comes from the terminology
# server checking SNOMED CT's own official ru designations, which never sees our local, unpublished
# package supplements regardless of designation or valueset-supplement wiring. So a ru gap is exactly
# as unfixable on our side as a uz gap - broadened to cover both.
# Safe to match this broadly: the message only fires when the display used IS valid for the default
# language, so it cannot mask a wrong code - that case is a separate "Wrong Display Name" message,
# which is deliberately not suppressed.
%for language(s) 'uz'. The display is%
%for language(s) 'ru'. The display is%


# Warning that can be ignored for our use since our instances use Uzbek/Russian but generated narrative in English
Resource has a language (uz), and the XHTML has a lang (en), but they differ
Resource has a language (ru), and the XHTML has a lang (en), but they differ
"The narrative includes custom content for the default language (en), but does not include translated content for the ru language, so the default language has been used
"The narrative includes custom content for the default language (en), but does not include translated content for the uz language, so the default language has been used

# Organization-scoped identifiers in this example follow dynamic pattern https://dhp.uz/fhir/core/sid/pid/uz/prn/{soliq-id}/{type}
# per identifiers.md in the main DHP IG (uz.dhp.core)
No definition could be found for URL value 'https://dhp.uz/fhir/core/sid/pid/uz/prn/200935935/%

# IG Publisher generates inline scripts for table filters then warns they're illegal - bug reported, can be ignored
The <script> containing the javascript '//%

# Screening: mimetypes value set is hosted externally and cannot be expanded by the publisher
Binding http://hl7.org/fhir/ValueSet/mimetypes|5.0.0 could not be expanded

# Suppressed in the main IG (uz.dhp.core) too: DHIS observations legitimately lack a performer.
Best Practice Recommendation: In general, all observations should have a performer

# Screening: body-site is too large for the publisher to verify subset membership
Unable to check if https://terminology.dhp.uz/fhir/integrations/ValueSet/screening-body-site-vs is a proper subset of http://hl7.org/fhir/ValueSet/body-site|5.0.0 - value set is too large to check
# Screening: breast-side VS (two SNOMED codes) is trivially a subset of screening-body-site-vs (all of SNOMED), but the parent is too large for the publisher to verify
Unable to check if https://terminology.dhp.uz/fhir/integrations/ValueSet/screening-ultrasound-breast-side-vs is a proper subset of https://terminology.dhp.uz/fhir/integrations/ValueSet/screening-body-site-vs - value set is too large to check

# IG Publisher 2.2.10 emits an OID-assignment recommendation for every CodeSystem/ValueSet. We don't use OID-based terminology (e.g. CDA), so assigning OIDs IG-wide adds no value here.
%should have an OID assigned to cater for possible use with OID based terminology systems%

# NOTE: core 0.6.0 re-bound Observation.method to a REQUIRED lab-method-vs, so our method VS is now
# flagged "not a subset" as a hard ERROR. That error is emitted during profile snapshot validation
# and is NOT suppressible here (ignoreWarnings does not filter this class - verified). It is
# tolerated instead via MAX_ALLOWED_ERRORS=1 in .github/workflows/validate-docs.yml, to be dropped
# to 0 once uz.dhp.core ships the extensible method binding (already in dev).

# dhis-body-site-vs includes the whole FHIR body-site VS, which is too large to expand.
# Trailing % => startsWith match: the real message has an embedded newline before its final ')',
# so an exact-match line never matches. Cut at (>1000) to stay independent of tx-server wording.
Unable to expand included value set 'http://hl7.org/fhir/ValueSet/body-site': The value set 'http://hl7.org/fhir/ValueSet/body-site|5.0.0' expansion has too many codes to produce (>1000)%

Error from https://tx.fhir.org/r5: Error: The code System "http://snomed.info/sct" has a grammar, and cannot be enumerated directly

Binding https://terminology.dhp.uz/fhir/integrations/ValueSet/screening-body-site-vs could not be expanded

Unable to expand included value set 'https://terminology.dhp.uz/fhir/integrations/ValueSet/screening-diagnostic-procedure-vs': Error from
https://tx.fhir.org/r5: Error: The code System "http://snomed.info/sct" has a grammar, and cannot be enumerated directly (and Error from
https://tx.fhir.org/r5: Error: The code System "http://snomed.info/sct" has a grammar, and cannot be enumerated directly )

# Form 111: registrationAndPersonalInformation legitimately accepts both plain lab-result Observations
# and UZCoreSocioeconomicObservation entries (education, working conditions, etc). Socioeconomic
# observations only add required constraints on top of base Observation, not on top of UZCoreObservation
# (they're sibling profiles, both derived straight from Observation), so a valid SocioeconomicObservation
# instance will always also structurally satisfy the looser UZCoreObservation. meta.profile already
# disambiguates correctly for consumers; narrowing the section's allowed types would incorrectly exclude
# the legitimate plain-Observation lab results that share the same section.
Found multiple matching profiles for urn:uuid:11100023-1111-2222-3333-444444444444 among choices: https://dhp.uz/fhir/core/StructureDefinition/uz-core-observation%

# Form 111: the DMEDForm111*CS source CodeSystems (input/fsh/terminology/DMEDForm111EnumCS.fsh) are
# deliberately declared content = #fragment - they preserve only the DMED tokens actually observed in
# the field-mapping evidence, not a guaranteed-complete enumeration of every token DMED could ever send.
# Declaring them #complete instead would be factually wrong and would risk the validator wrongly
# rejecting a real DMED token that just hasn't been catalogued yet. This warning is the expected,
# accepted cost of that (correct) choice, not a defect - see the ConceptMap file's DMEDForm111*CM
# instances, which all source from one of these fragment CodeSystems.
%doesn't have all content (content = fragment), so the source codes cannot be checked

# Form 111: several DMEDForm111*CM ConceptMap groups map DMED tokens straight to a handful of specific,
# hand-picked SNOMED CT codes (e.g. blood group, RhD, pregnancy outcome). SNOMED CT itself is too large
# for the publisher to load and check against, and defining a targetScope ValueSet for a one-off list of
# a few SNOMED codes would add a whole extra artifact for no real benefit over the explicit target codes
# already listed in the ConceptMap.
%is not fully defined and populated, and no targetScope is specified, so the target code checking will not be performed

# Form 111: DMEDForm111EducationTypeCM and DMEDForm111FamilyStatusCM each have a second ConceptMap group
# whose elements are entirely noMap = true (DMED tokens that intentionally map to nothing, per the
# integration doc's rule that an expected noMap token is not an unknown/error). A group with no mapped
# elements correctly has no group.target; this warning is the validator's inability to tell that apart
# from a forgotten target, not an actual missing target.
%No Target Code System, so the target codes cannot be checked
